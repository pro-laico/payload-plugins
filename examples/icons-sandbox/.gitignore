node_modules
.next
out

# Local SQLite DB (created at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# Uploaded files (written at runtime to local-disk storage). The icon collection's default
# static dir is `icon/` at the app root; `media/` covers any other upload collection.
/icon/
media/

# NOTE: seed-assets/svg/*.svg ARE committed — they're the small demo icons the seed uploads.

.env
.env*.local
next-env.d.ts
*.tsbuildinfo
