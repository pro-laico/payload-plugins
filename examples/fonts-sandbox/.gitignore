node_modules
.next
out

# Local SQLite DB (created at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# Upload storage written at runtime (raw + subsetted fonts)
fontOriginal/
fontOptimized/

# Generated by `pnpm generate:fonts` (next/font/local output). `definition.ts` is committed as an
# empty baseline (see the file) so the layout import resolves without a build; only `public/fonts/`
# is ignored.
public/fonts/

.env
.env*.local
next-env.d.ts
*.tsbuildinfo
