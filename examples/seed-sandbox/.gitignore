node_modules
.next
out

# Local SQLite DB (created at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# Uploaded media (written at runtime to local-disk storage). Root-anchored so the
# assets/media/ seed fixtures stay tracked.
/media/

.env
.env*.local
next-env.d.ts
*.tsbuildinfo
