// Component-local prop types. `Props` (from ./section-heading) is intentionally generic — its
// consumer imports it directly from '@/types/props/section-heading' to keep the root barrel clean.

export type { CmsIconProps } from './cms-icon'
export type { IconProps, IconSize, IconTone, IconVariant } from './icon'
export type { Props } from './section-heading'
