node_modules
.next
out

# Local SQLite DB (created at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# Uploaded media (written at runtime to local-disk storage). Root-anchored by convention.
/media/

# Local seed video files — uploaded to Mux from local, never committed
seed-assets/*
!seed-assets/.gitkeep

.env
.env*.local
next-env.d.ts
*.tsbuildinfo
