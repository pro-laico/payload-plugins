# @pro-laico/payload-dev-tools

A utility to help you build [Payload CMS](https://payloadcms.com/) projects faster: a floating dev toolbar, `/dev` pages inside your app, and a machine-readable app snapshot for AI agents. Flip your visitor's location to check consent behaviour, and boot against another env file with `payload-dev-env`. Dev-only, disappears in production.

```bash
pnpm add @pro-laico/payload-dev-tools
```

**Requires** Payload `^3`, React 19, and **Next.js 15+** (App Router). The toolbar and `/dev` pages are Next components; the whole surface is the UI, so there's no framework-agnostic subset.

**[Documentation →](https://payload-plugins.prolaico.com/docs/plugins/payload-dev-tools)**
